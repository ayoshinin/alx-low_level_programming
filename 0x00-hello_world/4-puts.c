#include <stdio.h>
/**
 * main- It is a primary point entry for puts function
 *
 * Return: By default it return zero if no errors it return non-zero value and it is a sucess.
 */
int main(void)
{
 puts("\"progtamming is like building a multilingual puzzle");
 return (0);
}
