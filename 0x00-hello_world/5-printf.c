#include <stdio.h>
/**
 * main- A program that write lines in a print function.
 * Return: It return zero by default if the function is a sucess.
 */
int main(void)
{
print("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
