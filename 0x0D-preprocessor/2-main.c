#include <stdio.h>

/**
 * main - It helps to print the name of a program file when complied,
 *        and it follows a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
