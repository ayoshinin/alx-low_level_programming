#include "main.h"
/**
 * _abs - It computes the absolute value of an integrer
 *
 * @n: checks integer.
 *
 * Return:  absolute value of integer.
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

