#include "main.h"
/**
 * _islower -  It helps to checks for lowercase char.
 *
 * @c:Checker character.
 * Return:  if c is lowercase,1 or otherwise 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

